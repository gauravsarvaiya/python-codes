gits command
 ---> git add .
 ---> git commit -m "comment enter"
 ----> git puch origin main

how to start:--

git init
git commit -m "added few files"
git config --global user.email "gauravsarvaiya786@gmail.com"
git config --global user.name "gauravsarvaiya"
git remote add origin https://github.com/gauravsarvaiya/python-codes.git(alocation address)